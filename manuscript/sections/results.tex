\section{Results}
\label{sec:results}

We evaluated DualBalance against the enacted 119th-Congress plan on all
multi-seat states for which TIGER 2020PL VTD boundaries are available.
California, Hawaii, and Oregon did not submit VTD data to the Census
Bureau and are excluded throughout (marked $\dagger$ in figures and
tables). The same data pipeline runs unmodified for every state via
\verb|scripts/prep_state_units.py|: TIGER 2020 VTDs, Census PL 94-171
demographics, dra2020/vtd\_data 2020 presidential returns, and a TIGER
2024 cd119 spatial join for the enacted plan. To anchor DualBalance in
context we score two additional deterministic algorithms: \emph{Cascade}
(\verb|src/dualbalance/cascade.py|), an Iowa-LSA-flavored construction
that aggregates VTDs to counties and uses farthest-point seeding; and
\emph{BDistricting}~\cite{olsonbd}, Brian Olson's published 50-state map
series ingested via Census 2020 Block Assignment Files
(\verb|scripts/prep_bdistricting.py|). All outputs are byte-identical
across repeated runs on the same input; the CLI integration test
\verb|test_generate_determinism_via_cli| pins this in CI.

Three cross-state findings organize the presentation.

\paragraph{Partisan asymmetry shrinks under every deterministic generator.}
On states with nonzero enacted efficiency gap, all three deterministic
algorithms produce smaller $|\mathrm{EG}|$ than the enacted plan
(Figure~\ref{fig:headline-eg}). The largest reductions appear in states
most associated with partisan-gerrymander litigation. The structural
explanation is in \S\ref{sec:discussion-forensic}: a generator that reads
no political data cannot reproduce a large partisan-fairness asymmetry.
This holds across all three deterministic baselines, not only DualBalance.

\paragraph{Cascade wins on DBS but is legally non-viable on most states.}
Cascade scores well on the DualBalance objective because county
aggregation naturally produces units of similar area, but the
county-integrity constraint yields population deviations far above the
\emph{Karcher} threshold on any state with a dominant metropolitan county
(Figure~\ref{fig:boxplots}, Panel B). DualBalance and BDistricting both
achieve \emph{Karcher} compliance on the large majority of states;
Cascade does so only on Iowa and Wisconsin, where no single county
exceeds the per-district population cap. A plan that wins on DBS but
violates \emph{Reynolds v.~Sims} cannot legally be enacted.

\paragraph{Different deterministic algorithms, different trade-offs.}
DualBalance holds $\mathrm{pop\_dev\_max}$ below 1\,\% on nearly all
states and recovers area balance through radial mixing of dense and sparse
territory, at the cost of lower Polsby-Popper compactness
(Figure~\ref{fig:boxplots}, Panels A and D). BDistricting maximizes
compactness and population balance but accepts high area imbalance because
Lloyd recentering explicitly minimizes within-district geographic spread.
Cascade maximizes county integrity and area balance but fails the
population-balance legal threshold on most states.

% -----------------------------------------------------------------------
% TABLE 1: Per-state DualBalance results (50 rows)
% -----------------------------------------------------------------------
\begin{table}[htbp]
\centering
\framebox[\textwidth]{\parbox[c][5cm]{0.9\textwidth}{\centering
\textbf{[PLACEHOLDER --- Table~\ref{tab:multistate-dbs}]}\\[6pt]
One row per state (50 rows). Columns: State, $N$, DualBalance Score
(DualBalance / Cascade / BDistricting / Enacted), $\mathrm{pop\_dev\_max}$
(DualBalance / Cascade). Bold marks the DBS winner per row.
Cascade rows flagged $\ddagger$ where $\mathrm{pop\_dev\_max} > 0.5\,\%$
(legally non-viable). States marked $\dagger$ excluded (no TIGER VTD data).\\[4pt]
Source: \texttt{out/compare\_all\_summary.json}.
}}
\caption{Per-state DualBalance results for all 50 states.
$\dagger$~State excluded (TIGER 2020PL VTD data not available).
$\ddagger$~$\mathrm{pop\_dev\_max} > 0.5\,\%$; plan does not satisfy
\emph{Reynolds v.~Sims} and is legally non-viable regardless of DBS.
\textbf{Bold} marks the highest DBS on each row.}
\label{tab:multistate-dbs}
\end{table}

% -----------------------------------------------------------------------
% TABLE 2: Aggregate algorithm comparison
% -----------------------------------------------------------------------
\begin{table}[htbp]
\centering
\framebox[\textwidth]{\parbox[c][5cm]{0.9\textwidth}{\centering
\textbf{[PLACEHOLDER --- Table~\ref{tab:aggregate-comparison}]}\\[6pt]
One row per metric, one column per algorithm (DualBalance / Cascade /
BDistricting / Enacted). Rows: DBS median, $\mathrm{pop\_dev\_max}$
median, states at \emph{Karcher} ($\leq 0.05\,\%$), states beating
enacted DBS, $|\mathrm{EG}|$ median, Polsby-Popper median. Bold marks
the best value per row.\\[4pt]
Source: \texttt{out/compare\_all\_summary.json}.
}}
\caption{Aggregate algorithm comparison across all available states.
\textbf{Bold} marks the best value in each row. ``At \emph{Karcher}''
counts states where $\mathrm{pop\_dev\_max} \leq 0.05\,\%$.
``Beats enacted DBS'' counts states where the algorithm's DualBalance
Score exceeds the enacted plan's.}
\label{tab:aggregate-comparison}
\end{table}

% -----------------------------------------------------------------------
% FIGURE 1: Headline EG comparison (all states, sorted by enacted |EG|)
% -----------------------------------------------------------------------
\begin{figure}[htbp]
\centering
\framebox[\textwidth]{\parbox[c][6cm]{0.9\textwidth}{\centering
\textbf{[PLACEHOLDER --- Figure~\ref{fig:headline-eg}]}\\[6pt]
Ranked bar chart: $|\mathrm{EG}|$ for DualBalance vs.\ enacted 119th-Congress plan,
one bar pair per state, sorted left-to-right by enacted $|\mathrm{EG}|$
(worst-gerrymandered on the left).
Red dashed line at $|\mathrm{EG}|=0.07$ (Stephanopoulos-McGhee threshold).
Source: \texttt{out/story\_headline\_eg.png}.
States without TIGER 2020PL VTD data excluded ($\dagger$).
}}
\caption{Partisan fairness across all available states, sorted by enacted
$|\mathrm{EG}|$ (worst-gerrymandered on the left). DualBalance (blue) vs.\
enacted 119th-Congress plan (gray). Red dashed line: $|\mathrm{EG}|=0.07$
gerrymander threshold~\cite{stephanopoulosmcghee2015}. States marked
$\dagger$ lacked TIGER 2020PL VTD boundaries and are excluded.}
\label{fig:headline-eg}
\end{figure}

% -----------------------------------------------------------------------
% FIGURE 2: Four-panel boxplots (DBS, pop_dev_max, |EG|, Polsby-Popper)
% -----------------------------------------------------------------------
\begin{figure}[htbp]
\centering
\framebox[\textwidth]{\parbox[c][8cm]{0.9\textwidth}{\centering
\textbf{[PLACEHOLDER --- Figure~\ref{fig:boxplots}]}\\[6pt]
$2\times2$ boxplot figure. Each panel: one box per algorithm
(DualBalance, Cascade, BDistricting, Enacted), one dot per state.\\[4pt]
\textbf{Panel A:} DualBalance Score (higher = better).\\
\textbf{Panel B:} $\mathrm{pop\_dev\_max}$, log scale; red dashed line at \emph{Karcher} 0.05\,\%.\\
\textbf{Panel C:} $|\mathrm{EG}|$ (lower = fairer); red dashed line at 0.07.\\
\textbf{Panel D:} Polsby-Popper mean (the honest compactness admission).\\[4pt]
Source: \texttt{out/story\_box\_*.png}.
}}
\caption{Cross-state comparison of four algorithms on key metrics.
Each box spans the interquartile range across all available states; dots are
individual states. \textbf{Panel A}: DualBalance Score (higher = better).
\textbf{Panel B}: maximum per-district population deviation, log scale;
dashed line at the \emph{Karcher} threshold (0.05\,\%).
\textbf{Panel C}: $|\mathrm{EG}|$ (lower = fairer); dashed line at the
0.07 gerrymander threshold~\cite{stephanopoulosmcghee2015}.
\textbf{Panel D}: Polsby-Popper compactness (mean per state); DualBalance
is structurally less compact than enacted plans because radial slices are
not blob-shaped.}
\label{fig:boxplots}
\end{figure}

% -----------------------------------------------------------------------
% FIGURE 3: Minority-majority district scatter
% -----------------------------------------------------------------------
\begin{figure}[htbp]
\centering
\framebox[\textwidth]{\parbox[c][6cm]{0.9\textwidth}{\centering
\textbf{[PLACEHOLDER --- Figure~\ref{fig:race-scatter}]}\\[6pt]
Scatter plot: minority-majority district count under DualBalance ($y$-axis)
vs.\ enacted plan ($x$-axis), one point per state, diagonal reference line
$y = x$. Points above the diagonal: DualBalance creates more MMDs than
enacted; below: fewer; on the line: tied.
Notable labeled points: NC (1$\to$2), TX (19$\to$22) above the diagonal;
MA (1$\to$0), WI (1$\to$0) below. Most states cluster at $(0,0)$.\\[4pt]
Source: computed from \texttt{out/compare\_all\_summary.json}.
}}
\caption{Minority-majority district count: DualBalance vs.\ enacted
119th-Congress plan. Each point is one state; the diagonal is $y = x$.
Points above the line indicate DualBalance produces more majority-minority
districts than the enacted map; points below indicate fewer. Most states
cluster at $(0,0)$ because no single minority group concentrates densely
enough to support a majority-minority district of standard size. DualBalance
is race-blind; where it produces more majority-minority districts than the
enacted plan, the effect is geographic, not by design. Where it produces
fewer (notably MA and WI), the enacted plan constructed a majority-minority
district that radial slicing breaks up.}
\label{fig:race-scatter}
\end{figure}

% -----------------------------------------------------------------------
% FIGURE 4: NC three-panel map
% -----------------------------------------------------------------------
\begin{figure}[htbp]
\centering
\framebox[\textwidth]{\parbox[c][6cm]{0.9\textwidth}{\centering
\textbf{[PLACEHOLDER --- Figure~\ref{fig:nc-maps}]}\\[6pt]
Three-panel side-by-side map: North Carolina (14 districts).\\
\textbf{Left:} Enacted 119th-Congress plan (EG $+0.20$, $\mathrm{pop\_dev\_max}$ 0.04\,\%).\\
\textbf{Center:} Cascade (EG $+0.03$, $\mathrm{pop\_dev\_max}$ 10.27\,\%, legally non-viable).\\
\textbf{Right:} DualBalance (EG $+0.09$, $\mathrm{pop\_dev\_max}$ 0.11\,\%).\\
Source: \texttt{out/maps\_NC.png}.
}}
\caption{North Carolina congressional districts under three plans (14 seats,
2020 PL 94-171). \textbf{Left:} enacted 119th-Congress plan, with the
Efficiency Gap of $+0.20$ that gave rise to \textit{Rucho v.~Common Cause}~\cite{rucho2019}.
\textbf{Center:} Cascade plan, which scores better on DBS ($0.82$ vs.\ $0.77$
enacted) but violates \emph{Karcher} at 10.27\,\% maximum population deviation
and cannot legally be enacted.
\textbf{Right:} DualBalance plan, \emph{Karcher}-compliant (0.11\,\%) and
reducing EG to $+0.09$ with no political input.}
\label{fig:nc-maps}
\end{figure}

\subsection{What would Congress look like?}
\label{sec:results-congress}

\textbf{[PLACEHOLDER, $\sim$150 words.]} Aggregate seats\_R / seats\_D
summed across all available states under DualBalance, Cascade,
BDistricting, and the enacted plan, plus a proportional-vote baseline
derived from the 2020 statewide two-party presidential returns. The
question, ``if every state used DualBalance, what is the partisan
composition of the House?'', is answered here with full caveats: 2020
presidential returns proxy for House votes, CA/HI/OR are excluded for
lack of VTD data, and single-seat states are unchanged under any
algorithm. The full per-state breakdown (seats R/D, statewide R share,
all four algorithms) appears in Supplementary Table~S1.
